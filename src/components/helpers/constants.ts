interface colorsInterface {
  background: string;
  inputBackground: string;
  textNormal: string;
}
export const colors: colorsInterface = {
  background: "#404749",
  inputBackground: "#1c2528",
  textNormal: "#28eb17",
};

interface whitespaceInterface {
  padding: string;
}
export const whitespace: whitespaceInterface = {
  padding: "2vmin",
};
